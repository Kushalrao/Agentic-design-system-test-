name: ds_lint
description: Custom lint rules for the Seasonal DLS — enforces token tier contracts at write time.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  analyzer: '>=6.0.0 <8.0.0'
  custom_lint_builder: ^0.6.0
